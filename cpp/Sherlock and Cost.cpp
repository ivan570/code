#include <bits/stdc++.h>
using namespace std;

int main() {
    int testcase;
    cin >> testcase;
    for (int T = 0; T < testcase; ++T) {
        int n;
        vector<int> arr(n); // B array
        for (int i = 0; i < n; ++i) {
            cin >> arr[i];
        }
        vector<int> ans(n); // A array
        if

    }
    return 0;
}
